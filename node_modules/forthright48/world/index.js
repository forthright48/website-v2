(function(){
    "use strict";

    var path = require ( "path" );

    var root = path.join ( __dirname, "../../.." );
    var secret = require ( path.join ( root, "/secret.js" ) );

    ///All related information that is require for layout: header
    var layout = {
        header : {
            // TODO: Send login information, to change header accordingly
        }
    };

    module.exports = {
        root: root,
        handleError: handleError,
        secret: secret,
        layout: layout

    };

    /*******************************************
    Implementation
    *******************************************/

    // Create an object wrapper for errors
    function handleError ( error, realError ) {
        return {
            subtitle: "error",
            error: error,
            realError: realError
        };
    }

}());
